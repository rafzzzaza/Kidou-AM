<!DOCTYPE html>
<html lang="id">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>am — activate</title>
<style>
:root {
  --bg: #0a0a0b;
  --panel: #111113;
  --line: #232326;
  --txt: #e8e8ea;
  --dim: #8a8a91;
  --acc: #5ec269;
  --acc-dim: #2c4a30;
  --red: #e05252;
  --blue: #5b8def;
  --blue-dim: #1e2a40;
  --glow: rgba(94,194,105,0.12);
}
* { margin: 0; padding: 0; box-sizing: border-box }
body {
  background: var(--bg);
  color: var(--txt);
  font-family: ui-monospace, 'Cascadia Code', Menlo, monospace;
  font-size: 13px;
  line-height: 1.6;
  min-height: 100vh;
  display: flex;
  justify-content: center;
  padding: 48px 16px;
}
.wrap { width: 100%; max-width: 800px }
header {
  border-bottom: 1px solid var(--line);
  padding-bottom: 14px;
  margin-bottom: 30px;
}
header h1 {
  font-size: 28px;
  font-weight: 600;
  letter-spacing: .02em;
  margin-bottom: 10px;
}
header h1 span { color: var(--acc) }
header p { color: var(--dim); font-size: 16px; }
.hero {
  margin-bottom: 40px;
  padding: 30px;
  background: var(--panel);
  border: 1px solid var(--line);
}
.hero h2 {
  font-size: 22px;
  margin-bottom: 15px;
}
.hero p {
  color: var(--dim);
  margin-bottom: 20px;
  line-height: 1.8;
}
.features {
  display: grid;
  gap: 20px;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  margin-bottom: 40px;
}
.feature {
  background: var(--panel);
  border: 1px solid var(--line);
  padding: 20px;
}
.feature h3 {
  font-size: 16px;
  margin-bottom: 10px;
  color: var(--acc);
}
.feature p {
  color: var(--dim);
  font-size: 14px;
}
.steps {
  display: flex;
  gap: 0;
  margin-bottom: 25px;
  font-size: 11px;
}
.steps div {
  flex: 1;
  padding: 6px 0;
  border-bottom: 2px solid var(--line);
  color: var(--dim);
  text-align: center;
}
.steps div.on { border-color: var(--acc); color: var(--txt) }
.steps div.done { border-color: var(--acc-dim); color: var(--acc) }
.panel {
  background: var(--panel);
  border: 1px solid var(--line);
  padding: 25px;
  margin-bottom: 20px;
}
.panel h2 {
  font-size: 18px;
  font-weight: 600;
  margin-bottom: 12px;
}
.panel .hint {
  color: var(--dim);
  font-size: 14px;
  margin-bottom: 18px;
}
label {
  display: block;
  color: var(--dim);
  font-size: 12px;
  margin-bottom: 6px;
  letter-spacing: .03em;
}
input {
  width: 100%;
  background: var(--bg);
  border: 1px solid var(--line);
  color: var(--txt);
  font: inherit;
  padding: 10px 12px;
  outline: none;
  margin-bottom: 12px;
}
input:focus { border-color: var(--acc) }
button {
  background: var(--acc);
  color: #06130a;
  border: none;
  font: inherit;
  font-weight: 600;
  padding: 10px 20px;
  cursor: pointer;
  margin-top: 10px;
  border-radius: 4px;
}
button.sec {
  background: transparent;
  color: var(--dim);
  border: 1px solid var(--line);
}
button:disabled { opacity: .45; cursor: wait }
.msg {
  padding: 10px 12px;
  font-size: 13px;
  margin: 12px 0;
  display: none;
  border-radius: 4px;
}
.msg.err { display: block; background: #2a1414; border: 1px solid #4a2020; color: var(--red) }
.msg.ok { display: block; background: #12201a; border: 1px solid var(--acc-dim); color: var(--acc) }
.kv {
  display: flex;
  justify-content: space-between;
  padding: 8px 0;
  border-bottom: 1px solid var(--line);
  font-size: 13px;
}
.kv:last-child { border-bottom: none }
.kv b { color: var(--dim); font-weight: 400 }
.kv span { color: var(--txt); text-align: right; word-break: break-all; max-width: 60% }
.tok {
  background: var(--bg);
  border: 1px solid var(--line);
  padding: 10px 12px;
  font-size: 11px;
  word-break: break-all;
  color: var(--dim);
  max-height: 100px;
  overflow: hidden;
  margin-top: 8px;
}
button.tiny {
  margin: 0;
  padding: 5px 10px;
  font-size: 12px;
}
footer {
  margin-top: 40px;
  color: #3f3f44;
  font-size: 12px;
  text-align: center;
  padding-top: 20px;
  border-top: 1px solid var(--line);
}
.hide { display: none }
/* Stats cards */
.stats {
  display: flex;
  gap: 20px;
  margin-bottom: 30px;
}
.stat {
  flex: 1;
  background: var(--panel);
  border: 1px solid var(--line);
  padding: 20px;
  text-align: center;
}
.stat h3 {
  font-size: 16px;
  margin-bottom: 8px;
  color: var(--dim);
}
.stat .value {
  font-size: 28px;
  font-weight: 600;
  color: var(--acc);
}
/* AUTO section */
.auto-panel {
  background: var(--panel);
  border: 2px solid var(--acc);
  border-radius: 8px;
  padding: 30px;
  margin-bottom: 24px;
  text-align: center;
}
.auto-panel h2 {
  font-size: 20px;
  margin-bottom: 8px;
  color: var(--acc);
}
.auto-panel p {
  color: var(--dim);
  margin-bottom: 20px;
  font-size: 14px;
}
.auto-btn {
  background: linear-gradient(135deg, var(--acc), #45a850);
  color: #06130a;
  border: none;
  font: inherit;
  font-size: 16px;
  font-weight: 700;
  padding: 14px 36px;
  cursor: pointer;
  border-radius: 6px;
  letter-spacing: .03em;
  transition: all .2s;
  box-shadow: 0 0 20px var(--glow);
}
.auto-btn:hover:not(:disabled) {
  transform: translateY(-1px);
  box-shadow: 0 0 30px var(--glow);
}
.auto-btn:disabled { opacity: .5; cursor: wait }
/* Auto progress */
.auto-progress { margin-top: 20px; text-align: left; }
.auto-progress .log {
  background: var(--bg);
  border: 1px solid var(--line);
  border-radius: 4px;
  padding: 12px;
  max-height: 250px;
  overflow-y: auto;
  font-size: 12px;
}
.auto-progress .log .entry {
  padding: 3px 0;
  color: var(--dim);
}
.auto-progress .log .entry .step {
  color: var(--acc);
  font-weight: 600;
}
.auto-progress .log .entry .step.err {
  color: var(--red);
}
.auto-progress .spinner {
  display: inline-block;
  width: 14px;
  height: 14px;
  border: 2px solid var(--dim);
  border-top-color: var(--acc);
  border-radius: 50%;
  animation: spin .8s linear infinite;
  margin-right: 6px;
  vertical-align: middle;
}
@keyframes spin { to { transform: rotate(360deg) } }
/* Responsive */
@media (max-width: 600px) {
  .stats { flex-direction: column; }
  .features { grid-template-columns: 1fr; }
  .auto-panel { padding: 20px; }
}
</style>
</head>
<body>
<div class="wrap">
<header>
  <h1>am <span>/</span> activate <span style="color:var(--dim);font-weight:400">by rafa</span></h1>
  <p>alight motion premium — magic link activation</p>
</header>

<div class="hero">
  <h2>What is this?</h2>
  <p>Automated activation service for Alight Motion premium using magic links. One-click auto mode or manual flow — your choice.</p>
</div>

<div class="stats">
  <div class="stat">
    <h3>Total Activations</h3>
    <div class="value" id="st-total">-</div>
  </div>
  <div class="stat">
    <h3>Today</h3>
    <div class="value" id="st-today">-</div>
  </div>
</div>

<!-- AUTO MODE -->
<div class="auto-panel">
  <h2>&#9889; Auto Premium</h2>
  <p>Generate temp email → kirim link → ambil dari inbox → verifikasi → premium aktif.<br>Satu klik, tanpa perlu API.</p>
  <button class="auto-btn" id="autoBtn">&#9654; Jalankan Otomatis</button>
  <div class="auto-progress hide" id="autoProg">
    <div class="log" id="autoLog"></div>
  </div>
</div>

<!-- AUTO RESULT -->
<div class="panel hide" id="autoResult">
  <h2 style="color:var(--acc)">premium aktif</h2>
  <p class="hint">detail akun di bawah, token bisa dicopy.</p>
  <div class="kv"><b>email</b><span id="ar-em">-</span></div>
  <div class="kv"><b>uid</b><span id="ar-uid">-</span></div>
  <div class="kv"><b>order</b><span id="ar-ord">-</span></div>
  <div class="kv"><b>valid until</b><span id="ar-until">-</span></div>
  <div style="margin-top:12px">
    <label>id token</label>
    <div class="tok" id="ar-tok">-</div>
    <button class="tiny sec" id="ar-copy">copy token</button>
    <button class="tiny sec" id="ar-again">jalankan lagi</button>
  </div>
</div>

<!-- DIVIDER -->
<div style="margin:30px 0;border-top:1px solid var(--line);padding-top:20px">
  <p style="color:var(--dim);font-size:14px;margin-bottom:16px">atau gunakan <b style="color:var(--txt)">mode manual</b> — masukkan email & link sendiri:</p>
</div>

<!-- MANUAL MODE -->
<div class="steps">
  <div id="n1" class="on">01 email</div>
  <div id="n2">02 link</div>
  <div id="n3">03 done</div>
</div>

<div class="panel" id="p1">
  <h2>masukkan email</h2>
  <p class="hint">link login dikirim ke email, tanpa password.</p>
  <label>email</label>
  <input id="em" type="email" placeholder="nama@email.com">
  <button id="b1">kirim link &rarr;</button>
  <div id="m1" class="msg"></div>
</div>

<div class="panel hide" id="p2">
  <h2>tempel link</h2>
  <p class="hint">link terkirim ke <span id="w-em" style="color:var(--txt)">-</span>. copy link dari email, paste di sini.</p>
  <label>link verifikasi</label>
  <input id="lk" type="text" placeholder="https://... atau oobCode">
  <button id="b2">&larr; kembali</button>
  <button id="b3">verifikasi &rarr;</button>
  <div id="m2" class="msg"></div>
</div>

<div class="panel hide" id="p3">
  <h2 style="color:var(--acc)">premium aktif</h2>
  <p class="hint">detail akun di bawah, token bisa dicopy.</p>
  <div class="kv"><b>email</b><span id="r-em">-</span></div>
  <div class="kv"><b>uid</b><span id="r-uid">-</span></div>
  <div class="kv"><b>order</b><span id="r-ord">-</span></div>
  <div style="margin-top:12px">
    <label>id token</label>
    <div class="tok" id="r-tok">-</div>
    <button class="tiny sec" id="b4">copy token</button>
    <button class="tiny sec" id="b5">aktivasi lain</button>
  </div>
</div>

<div class="features">
  <div class="feature">
    <h3>One-Click Auto</h3>
    <p>Tombol otomatis: generate temp email, kirim link, ambil dari inbox, verifikasi &mdash; selesai.</p>
  </div>
  <div class="feature">
    <h3>Manual Mode</h3>
    <p>Masukkan email & link sendiri kalau ingin kendali penuh. Langkah demi langkah.</p>
  </div>
  <div class="feature">
    <h3>Secure & Private</h3>
    <p>Temp mail langsung dihapus. Tidak ada data yang disimpan permanen.</p>
  </div>
</div>

<footer>unofficial &middot; reverse engineered &middot; use at your own risk</footer>
</div>

<script>
const $ = i => document.getElementById(i)

// Stats
fetch('/api/stats').then(r => r.json()).then(j => {
  $('st-total').textContent = Number(j.total || 0).toLocaleString('id-ID')
  $('st-today').textContent = Number(j.today || 0).toLocaleString('id-ID')
}).catch(() => {})

// === MANUAL MODE ===
const step = n => {
  for (let i = 1; i <= 3; i++) {
    $('p' + i).classList.toggle('hide', i !== n)
    $('n' + i).className = i < n ? 'done' : (i === n ? 'on' : '')
  }
}
const say = (id, txt, ok) => {
  const m = $(id)
  m.textContent = txt
  m.className = 'msg ' + (ok ? 'ok' : 'err')
}
const busy = (b, on, t) => { b.disabled = on; if (t) b.textContent = t }

$('b1').onclick = async () => {
  const em = $('em').value.trim()
  if (!em.includes('@')) return say('m1', 'email gak valid', 0)
  const b = $('b1'); busy(b, true, 'mengirim...')
  try {
    const r = await fetch('/api/send-link', { method: 'POST', headers: {'content-type':'application/json'}, body: JSON.stringify({ email: em }) })
    const j = await r.json()
    if (j.success) { say('m1', 'link terkirim, cek email', 1); $('w-em').textContent = em; setTimeout(() => step(2), 700) }
    else say('m1', j.message || 'gagal', 0)
  } catch { say('m1', 'server gak nyambung', 0) }
  busy(b, false); b.textContent = 'kirim link \u2192'
}

$('b2').onclick = () => step(1)

$('b3').onclick = async () => {
  const raw = $('lk').value.trim()
  if (!raw) return say('m2', 'link kosong', 0)
  const b = $('b3'); busy(b, true, 'verifikasi...')
  try {
    const r = await fetch('/api/verify-link', { method: 'POST', headers: {'content-type':'application/json'}, body: JSON.stringify({ email: $('w-em').textContent, magicLink: raw }) })
    const j = await r.json()
    if (j.success) {
      const d = j.data
      $('r-em').textContent = d.email
      $('r-uid').textContent = d.uid
      $('r-ord').textContent = d.orderId || '-'
      $('r-tok').textContent = d.idToken
      window.__tok = d.idToken
      step(3)
    } else say('m2', j.message || 'gagal', 0)
  } catch { say('m2', 'server gak nyambung', 0) }
  busy(b, false); b.textContent = 'verifikasi \u2192'
}

$('b4').onclick = () => { navigator.clipboard.writeText(window.__tok || ''); $('b4').textContent = 'copied' }
$('b5').onclick = () => { $('lk').value = ''; step(1) }

// === AUTO MODE ===
const addLog = (step, msg, isErr) => {
  const log = $('autoLog')
  const entry = document.createElement('div')
  entry.className = 'entry'
  const icon = isErr ? '\u2718' : '\u2714'
  entry.innerHTML = `<span class="step${isErr ? ' err' : ''}">${icon} ${step}</span> — ${msg}`
  log.appendChild(entry)
  log.scrollTop = log.scrollHeight
}

$('autoBtn').onclick = async () => {
  const btn = $('autoBtn')
  const prog = $('autoProg')
  const log = $('autoLog')
  const result = $('autoResult')

  btn.disabled = true
  btn.innerHTML = '<span class="spinner"></span> berjalan...'
  prog.classList.remove('hide')
  result.classList.add('hide')
  log.innerHTML = ''

  addLog('init', 'memulai otomasi...')

  try {
    const r = await fetch('/api/auto', { method: 'POST', headers: {'content-type':'application/json'}, body: '{}' })
    const j = await r.json()

    // Show server logs
    if (j.log) {
      j.log.forEach(l => addLog(l.step, l.message, l.step === 'error'))
    }

    if (j.success) {
      const d = j.data
      $('ar-em').textContent = d.email
      $('ar-uid').textContent = d.uid
      $('ar-ord').textContent = d.orderId || '-'
      $('ar-until').textContent = d.validUntil || '-'
      $('ar-tok').textContent = d.idToken
      window.__autoTok = d.idToken
      result.classList.remove('hide')

      // Refresh stats
      fetch('/api/stats').then(r => r.json()).then(j => {
        $('st-total').textContent = Number(j.total || 0).toLocaleString('id-ID')
        $('st-today').textContent = Number(j.today || 0).toLocaleString('id-ID')
      }).catch(() => {})

      btn.innerHTML = '\u2714 selesai!'
      btn.style.background = 'var(--acc)'
    } else {
      addLog('error', j.message || 'gagal', true)
      btn.innerHTML = '\u2718 gagal — coba lagi'
      btn.style.background = 'var(--red)'
    }
  } catch (e) {
    addLog('error', 'server gak nyambung', true)
    btn.innerHTML = '\u2718 error'
    btn.style.background = 'var(--red)'
  }

  btn.disabled = false
  setTimeout(() => {
    btn.innerHTML = '\u25B6 Jalankan Otomatis'
    btn.style.background = ''
  }, 3000)
}

$('ar-copy').onclick = () => { navigator.clipboard.writeText(window.__autoTok || ''); $('ar-copy').textContent = 'copied' }
$('ar-again').onclick = () => { $('autoResult').classList.add('hide'); $('autoLog').innerHTML = ''; $('autoProg').classList.add('hide') }
</script>
</body>
</html>
